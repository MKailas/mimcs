Configuration
=============

The handful of settings that depend on the machine rather than the model: the row-chunking byte
budget and x64. See ``design/15_configuration.md`` for why these two and not others.

.. automodule:: mimcs.config
   :members:

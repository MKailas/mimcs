Models and parameters
=====================

What a density is written over: `Model` plus one module per parameter type. A continuous
parameter is defined by its charts; a discrete (integer) one has none --- sample space is
coordinate space --- and lives in a separate list with its own flat `int` layout.

.. automodule:: mimcs.model
   :members:
   :show-inheritance:
